services:
  smt:
    build: .
    volumes:
      - ./smt:/code/smt
    container_name: smt-backend
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    command: ["python", "/code/smt/main.py"]
    restart: on-failure
